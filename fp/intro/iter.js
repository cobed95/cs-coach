/**
 * 한 함수 f와 정수 a, 횟수 n을 받아 함수 f를 a에 대해 n 번 적용하는 함수 iter를 작성하시오.
 * 예를들어 iter(f, 0, a)는 a를 되돌려준다.
 * iter(f, 1, a)는 f(a)를 되돌려준다.
 * iter(f, 2, a)는 f(f(a))를 되돌려준다.
 * iter(f, 3, a)는 f(f(f(a)))를 되돌려준다.
 * ...
 */
const iter = (f, a, n) => {
    // TODO: 구현 ㄱㄱ!
}
